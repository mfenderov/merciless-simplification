name: release

# Issue a new version whenever the methodology, skill, or extension changes:
# bump patch version, tag vX.Y.Z, push the tag, publish to npm.
# Guarded so the release commit itself does not re-trigger the workflow.

on:
  push:
    branches: [main]
    paths:
      - "extensions/**"
      - "skills/**"
      - "content/_index.md"
      - "README.md"
      - "package.json"

permissions:
  contents: write
  id-token: write

jobs:
  release:
    if: ${{ github.event.head_commit.author.name != 'github-actions[bot]' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          registry-url: "https://registry.npmjs.org"

      - name: Bump patch version
        id: bump
        run: npm version patch --no-git-tag-version

      - name: Commit, tag, and push release
        run: |
          VERSION=$(node -p "require('./package.json').version")
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package.json
          git commit -m "chore: release v${VERSION}"
          git tag "v${VERSION}"
          git push origin HEAD:main
          git push origin "v${VERSION}"

      # Tokenless publish via npm trusted publishers (OIDC). Requires the
      # trusted publisher configured on npmjs.com for this repo + release.yml;
      # provenance attestations are generated automatically.
      - name: Publish to npm
        run: npm publish --access public
